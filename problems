import { getPriority } from "os";

mongoose.connect(CONNECTION_URL, {useNewUrlParser:true, useUnifiedTopology:true}).then(()=>app.listen(PORT,() => console.log(`This Port Runnig at: ${PORT}`))).catch((error)=>console.log(error.));

mongoose.connect(CONNECTION_URL, {useNewUrlParser:true, useUnifiedTopology:true}).then(()=>app.listen(PORT,() => console.log(`This Port Runnig at: ${PORT}`))).catch((error)=>console.log(error.));
2)
Could not connect to any servers in your MongoDB Atlas cluster. One common reason is that you're trying to access the 
database from an IP that isn't whitelisted. Make sure your 
current IP address is on your Atlas cluster's IP whitelist: https://docs.atlas.mongodb.com/security-whitelist/  

3)
git remote add origin https://github.com/Npradheepan/Node_React-Back_Front-mongod-1b.git
git branch -M main
git push -u origin main